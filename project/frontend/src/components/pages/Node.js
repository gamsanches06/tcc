import styles from './Node.module.css'

function Node() {
    return (
        <section className={styles.node_container}>
         <button>
                Fazer requisição Node
        </button>
        </section>
    )
}

export default Node